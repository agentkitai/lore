"""Shared argument parsing helpers for the Lore CLI."""

from __future__ import annotations

# This module is reserved for shared argparse argument-group builders.
# Currently, argument construction is done inline in build_parser().
# As common argument patterns emerge, extract them here.
